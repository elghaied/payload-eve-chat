DATABASE_URL=mongodb://127.0.0.1/your-database-name
PAYLOAD_SECRET=YOUR_SECRET_HERE

# --- Eve agent (Vercel Eve framework) ---
EVE_MODEL=llama-3.3-70b-versatile     # Groq model id (must support tool calling)
GROQ_API_KEY=                         # put your real key in .env.local (gitignored), not here
MCP_SERVER_URL=http://localhost:3000/api/mcp
MCP_API_KEY=                          # optional in dev; required in production

# The previous Vercel-AI-SDK setup (AI_PROVIDER / ANTHROPIC_API_KEY / OPENAI_API_KEY /
# OLLAMA_* / voice STT_*/TTS_* / SEARXNG_*) applies only to the `ai-sdk` branch.
